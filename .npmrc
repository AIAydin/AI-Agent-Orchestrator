# Keep credentials in the user-level ~/.npmrc or GitHub Actions secrets.
# This project file is public and intentionally contains policy only.
auto-install-peers=false
engine-strict=true
save-exact=true
strict-peer-dependencies=true
